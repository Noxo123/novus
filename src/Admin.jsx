import React,{useEffect,useMemo,useState} from 'react';
import './admin.css';
const api=async(path,opts={})=>{const r=await fetch(path,{credentials:'same-origin',...opts,headers:{'Content-Type':'application/json',...(opts.headers||{})}});const d=r.status===204?{}:await r.json();if(!r.ok)throw Error(d.error||'Erreur');return d};
const fmtBytes=n=>{if(!n)return '0 B';const u=['B','KB','MB','GB'];let i=0,x=n;while(x>=1024&&i<u.length-1){x/=1024;i++}return `${x.toFixed(i?1:0)} ${u[i]}`};
export default function Admin(){const [logged,setLogged]=useState(false),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[csrf,setCsrf]=useState(''),[data,setData]=useState(null),[audit,setAudit]=useState([]),[error,setError]=useState(''),[loading,setLoading]=useState(true),[tab,setTab]=useState('dashboard'),[search,setSearch]=useState(''),[account,setAccount]=useState({email:'',password:'',role:'USER',displayName:''}),[form,setForm]=useState({name:'',version:'',description:''}),[update,setUpdate]=useState(null);
const load=async()=>{try{const d=await api('/api/admin/overview');const a=await api('/api/admin/audit');setData(d);setAudit(a.events||[]);setLogged(true)}catch(e){if(e.message!=='ADMIN_AUTH_REQUIRED')setError(e.message)}finally{setLoading(false)}};useEffect(()=>{load()},[]);
const login=async e=>{e.preventDefault();setError('');try{const d=await api('/api/admin/login',{method:'POST',body:JSON.stringify({email,password})});setCsrf(d.csrf);setPassword('');await load()}catch(e){setError(e.message)}};
const act=async(path,method='POST',body)=>{setError('');try{await api(path,{method,body:body?JSON.stringify(body):undefined,headers:{'x-csrf-token':csrf}});await load()}catch(e){setError(e.message)}};
const stats=useMemo(()=>{const p=data?.plugins||[];return {total:p.length,approved:p.filter(x=>x.status==='approved'&&!x.archived).length,pending:p.filter(x=>x.status==='pending').length,archived:p.filter(x=>x.archived).length,accounts:data?.accounts?.length||0,quarantine:data?.quarantineCount||0}},[data]);
const filtered=(data?.plugins||[]).filter(p=>`${p.name} ${p.author} ${p.status}`.toLowerCase().includes(search.toLowerCase()));
const revisionAction=async(r,action)=>act(`/api/admin/plugins/${r.pluginId}/revisions/${r.id}`,'POST',{action});
if(loading)return <div className="admin-shell"><div className="admin-loading">NOVUS ADMIN · SECURE BOOT…</div></div>;
if(!logged)return <div className="admin-shell"><div className="login-card"><div className="brand"><span className="brand-mark">N</span> NOVUS ADMIN</div><p>Console d'administration protégée. Sessions HttpOnly, rate-limit et journalisation active.</p><form onSubmit={login}><input autoFocus type="email" placeholder="Email administrateur" value={email} onChange={e=>setEmail(e.target.value)}/><input type="password" placeholder="Mot de passe" value={password} onChange={e=>setPassword(e.target.value)}/><button className="button primary">Authentifier →</button></form>{error&&<div className="admin-error">⚠ {error}</div>}</div></div>;
return <div className="admin-shell"><header className="admin-nav"><a className="brand" href="/"><span className="brand-mark">N</span> NOVUS <span className="admin-badge">{data?.accounts?.find(x=>x.email===email)?.role||'CONTROL'}</span></a><div className="admin-health"><i/> CONTROL PLANE · LIVE</div><button className="button ghost" onClick={async()=>{await api('/api/admin/logout',{method:'POST',headers:{'x-csrf-token':csrf}});setLogged(false)}}>Déconnexion</button></header><main className="admin-main"><div className="admin-heading"><div><span>CONTROL CENTER / GOVERNANCE</span><h1>Administration <em>NOVUS.</em></h1><p>Surveillance, comptes, plugins, révisions communautaires, quarantaine, sécurité et mises à jour.</p></div><div className="security-score"><strong>HARDENED</strong><small>AUTH · CSRF · RATE LIMIT · AUDIT</small></div></div>{error&&<div className="admin-error">⚠ {error}</div>}
<div className="admin-tabs">{[['dashboard','📊 Dashboard'],['plugins','🔌 Plugins'],['revisions','🧪 Révisions'],['accounts','👥 Comptes'],['security','🔐 Sécurité'],['audit','🧾 Audit'],['system','⚙️ Système']].map(([id,label])=><button key={id} className={tab===id?'active':''} onClick={()=>setTab(id)}>{label}</button>)}</div>
{tab==='dashboard'&&<><section className="metric-grid">{[['Plugins',stats.total],['Validés',stats.approved],['En revue',stats.pending],['Archivés',stats.archived],['Quarantaine',stats.quarantine],['Comptes',stats.accounts]].map(([k,v])=><div className="metric" key={k}><span>{k}</span><strong>{v}</strong></div>)}</section><section className="admin-grid"><div className="admin-panel"><div className="panel-title"><span>SERVER TELEMETRY</span><b>LIVE</b></div><div className="security-list"><div>⏱️ Uptime <strong>{Math.floor((data?.stats?.uptime||0)/3600)}h {Math.floor((data?.stats?.uptime||0)%3600/60)}m</strong></div><div>🧠 RAM process <strong>{fmtBytes(data?.stats?.memory?.rss)}</strong></div><div>💾 Heap <strong>{fmtBytes(data?.stats?.memory?.heapUsed)} / {fmtBytes(data?.stats?.memory?.heapTotal)}</strong></div><div>🖥️ CPU <strong>{data?.stats?.cpu?.cores} cores · load {data?.stats?.cpu?.load?.[0]?.toFixed(2)}</strong></div><div>🟢 Node <strong>{data?.stats?.node}</strong></div><div>🌐 Environment <strong>{data?.stats?.env}</strong></div><div>🔌 Bind <strong>0.0.0.0:{data?.stats?.port}</strong></div></div></div><div className="admin-panel"><div className="panel-title"><span>INTEGRATIONS</span><b>STATUS</b></div><div className="security-list"><div>🐙 GitHub <strong>{data?.githubConfigured?'CONNECTED':'NOT CONFIGURED'}</strong></div><div>💬 Discord <strong>{data?.discordConfigured?'CONNECTED':'NOT CONFIGURED'}</strong></div><div>🔄 Auto-update <strong>{data?.autoUpdate?'ENABLED':'DISABLED'}</strong></div><div>🌿 Branch <strong>{data?.updateBranch}</strong></div><div>🧪 Pending reviews <strong>{data?.pendingRevisions?.length||0}</strong></div></div><button className="button primary" onClick={async()=>{try{setUpdate(await api('/api/admin/update/check',{method:'POST',headers:{'x-csrf-token':csrf}}))}catch(e){setError(e.message)}}}>Vérifier GitHub</button>{update&&<div className="empty">{update.upToDate?'✓ Production à jour':'⚠ Une mise à jour est disponible'}<br/><small>{update.local?.slice(0,8)} → {update.remote?.slice(0,8)}</small></div>}</div></section></>}
{tab==='plugins'&&<section className="admin-panel"><div className="panel-title"><span>PLUGIN REGISTRY</span><b>{filtered.length} PLUGINS</b></div><div className="plugin-form"><input placeholder="Nom du plugin" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input placeholder="Version" value={form.version} onChange={e=>setForm({...form,version:e.target.value})}/><input placeholder="Description" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/><button className="button primary" onClick={async()=>{await act('/api/admin/plugins','POST',form);setForm({name:'',version:'',description:''})}}>+ Ajouter</button></div><input className="admin-search" placeholder="Rechercher…" value={search} onChange={e=>setSearch(e.target.value)}/><div className="plugin-list">{filtered.map(p=><div className="plugin-row" key={p.id}><div><strong>{p.name}</strong><small>v{p.version||'—'} · {p.author} · {p.revisionCount} révisions</small><p>{p.description}</p></div><select value={p.status} onChange={e=>act(`/api/admin/plugins/${p.id}`,'PATCH',{status:e.target.value})}><option value="pending">REVIEW</option><option value="approved">APPROVED</option><option value="maintenance">MAINTENANCE</option><option value="rejected">REJECTED</option></select><button className="button ghost" onClick={()=>act(`/api/admin/plugins/${p.id}`,'PATCH',{archived:!p.archived})}>{p.archived?'Désarchiver':'Archiver'}</button><button className="danger" onClick={()=>act(`/api/admin/plugins/${p.id}`,'DELETE')}>×</button></div>)}</div></section>}
{tab==='revisions'&&<section className="admin-panel"><div className="panel-title"><span>COMMUNITY REVIEW QUEUE</span><b>{data?.pendingRevisions?.length||0} PENDING</b></div><p className="muted">Toute modification communautaire arrive ici. L'approbation applique la révision au plugin et le remet en état validé.</p><div className="plugin-list">{(data?.pendingRevisions||[]).map(r=><div className="plugin-row" key={r.id}><div><strong>{r.pluginName}</strong><small>{r.type==='edit'?'Modification':'Création'} · v{r.version||'—'} · {new Date(r.createdAt).toLocaleString('fr-FR')}</small><p>{r.changes?.description||r.description||'Nouvelle proposition'}</p></div><button className="button primary" onClick={()=>revisionAction(r,'approve')}>✓ Tester / approuver</button><button className="button ghost" onClick={()=>revisionAction(r,'reject')}>Rejeter</button></div>)}{!data?.pendingRevisions?.length&&<div className="empty">Aucune révision en attente.</div>}</div></section>}
{tab==='accounts'&&<section className="admin-grid"><div className="admin-panel"><div className="panel-title"><span>ACCOUNT MANAGEMENT</span><b>{data?.accounts?.length||0}</b></div><div className="plugin-form"><input placeholder="Nom / pseudo" value={account.displayName} onChange={e=>setAccount({...account,displayName:e.target.value})}/><input type="email" placeholder="Email" value={account.email} onChange={e=>setAccount({...account,email:e.target.value})}/><input type="password" placeholder="Mot de passe · 12 min" value={account.password} onChange={e=>setAccount({...account,password:e.target.value})}/><select value={account.role} onChange={e=>setAccount({...account,role:e.target.value})}><option>USER</option><option>MODERATOR</option><option>ADMIN</option><option>OWNER</option></select><button className="button primary" onClick={async()=>{await act('/api/admin/accounts','POST',account);setAccount({email:'',password:'',role:'USER',displayName:''})}}>Créer</button></div></div><div className="admin-panel"><div className="panel-title"><span>MEMBERS</span><b>ACTIVE / DISABLED</b></div><div className="plugin-list">{(data?.accounts||[]).map(u=><div className="plugin-row" key={u.id}><div><strong>{u.displayName} · {u.email}</strong><small>{u.role} · {new Date(u.createdAt).toLocaleDateString('fr-FR')}</small></div><button className="button ghost" onClick={()=>act(`/api/admin/accounts/${u.id}`,'PATCH',{enabled:!u.enabled})}>{u.enabled?'Désactiver':'Activer'}</button><button className="danger" onClick={()=>act(`/api/admin/accounts/${u.id}`,'DELETE')}>×</button></div>)}</div></div></section>}
{tab==='security'&&<section className="admin-grid"><div className="admin-panel"><div className="panel-title"><span>SECURITY POSTURE</span><b>LIVE</b></div><div className="security-list"><div>🔐 Sessions <strong>8h · HttpOnly · SameSite</strong></div><div>🛡️ CSRF <strong>ACTIVE</strong></div><div>⏱️ Admin login limit <strong>8 / 10 min</strong></div><div>👤 Registration limit <strong>5 / hour / IP</strong></div><div>🔑 Password storage <strong>scrypt + random salt</strong></div><div>📦 File permissions <strong>0600</strong></div><div>🐙 GitHub token <strong>{data?.githubConfigured?'CONFIGURED':'MISSING'}</strong></div><div>💬 Discord webhook <strong>{data?.discordConfigured?'CONFIGURED':'MISSING'}</strong></div><div>👑 OWNER <strong>{(data?.accounts||[]).filter(x=>x.role==='OWNER'&&x.enabled).length}</strong></div></div></div><div className="admin-panel"><div className="panel-title"><span>QUICK ACCOUNT</span><b>{data?.quickCreateEnabled?'ENABLED':'DISABLED'}</b></div><p className="muted">Création rapide réservée à l'administration. Le compte communautaire normal reste disponible sur /plugins.</p><button className="button ghost" onClick={()=>act('/api/admin/quick-create/toggle','POST',{enabled:!data?.quickCreateEnabled})}>{data?.quickCreateEnabled?'Désactiver':'Réactiver'} Quick Account</button></div></section>}
{tab==='audit'&&<section className="admin-panel"><div className="panel-title"><span>AUDIT TRAIL</span><b>LAST 500</b></div><div className="audit-list">{audit.map((x,i)=><div key={i}><time>{new Date(x.at).toLocaleString('fr-FR')}</time><span>{x.action}</span><small>{x.email||x.by||x.pluginId||x.id||''}</small></div>)}</div></section>}
{tab==='system'&&<section className="admin-grid"><div className="admin-panel"><div className="panel-title"><span>QUARANTINE</span><b>{data?.quarantineCount||0} FILES</b></div><div className="quarantine-list">{(data?.quarantine||[]).map(x=><div key={x.name}><span>🔒 {x.name}</span><small>{fmtBytes(x.size)}</small></div>)}{!data?.quarantine?.length&&<div className="empty">Quarantaine vide.</div>}</div><button className="button danger-wide" onClick={()=>act('/api/admin/quarantine/purge')}>Vider la quarantaine</button></div><div className="admin-panel"><div className="panel-title"><span>UPDATES</span><b>{data?.autoUpdate?'AUTO':'MANUAL'}</b></div><p className="muted">Branche : {data?.updateBranch}. Vérifie d'abord puis applique seulement après contrôle.</p><button className="button primary" onClick={async()=>{try{setUpdate(await api('/api/admin/update/check',{method:'POST',headers:{'x-csrf-token':csrf}}))}catch(e){setError(e.message)}}}>Vérifier</button><button className="button ghost" onClick={()=>act('/api/admin/update/apply')}>Appliquer la mise à jour</button></div></section>}
<div className="admin-footer"><span>Repository : <strong>{data?.githubRepo}</strong></span><span>Community changes → review → validation → publication.</span></div></main></div>}
