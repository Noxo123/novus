import React,{useState} from 'react';
import {createRoot} from 'react-dom/client';
import Admin from './Admin.jsx';
import Community from './Community.jsx';
import './styles.css';

const features=[['🧠','Communauté first','Les joueurs proposent, développent et font évoluer l’univers.'],['🤖','Analyse automatisée','Chaque contribution passe par des contrôles de sécurité et de qualité.'],['🧪','Staging','Rien n’arrive directement en production : on teste avant de publier.'],['🌙','21h30 Release','Un rendez-vous quotidien pour découvrir les nouveautés validées.']];
const checks=[['Malware & secrets','🟢','Aucun indicateur critique détecté'],['Réseau & fichiers','🟢','Accès sensibles vérifiés'],['Performance','🟡','Review humaine recommandée'],['Tests','🟢','Pipeline terminé avec succès']];
function App(){const[file,setFile]=useState(null),[author,setAuthor]=useState(''),[status,setStatus]=useState(null),[menu,setMenu]=useState(false);const chooseFile=f=>{if(!f)return;if(!/\.(jar|zip)$/i.test(f.name))return setStatus({type:'error',message:'Seuls les fichiers .jar et .zip sont acceptés.'});if(f.size>10*1024*1024)return setStatus({type:'error',message:'Le fichier dépasse la limite de 10 Mo.'});setFile(f);setStatus(null)};const submit=async()=>{if(!file)return;setStatus({type:'loading',message:'Envoi vers la quarantaine NOVUS…'});try{const data=await new Promise((ok,no)=>{const r=new FileReader();r.onload=()=>ok(String(r.result).split(',')[1]);r.onerror=no;r.readAsDataURL(file)});const r=await fetch('/api/contributions',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({filename:file.name,data,author})});const d=await r.json();if(!r.ok)throw Error(d.error||'Échec de l’envoi.');setStatus({type:'success',message:'Contribution reçue et placée en quarantaine.',result:d})}catch(e){setStatus({type:'error',message:e.message||'Impossible d’envoyer la contribution.'})}};return <div className="app"><header className="nav"><a className="brand" href="#top"><span className="brand-mark">N</span> NOVUS</a><nav className={menu?'nav-links open':'nav-links'}><a href="#vision">Le concept</a><a href="#security">Sécurité</a><a href="/plugins">Plugins</a><a href="#upload">Contribuer</a><a href="#release">Releases</a><a href="/admin">Admin</a></nav><a className="github" href="https://github.com/Noxo123/novus-communauter" target="_blank" rel="noreferrer">Communauté GitHub ↗</a><button className="menu" onClick={()=>setMenu(!menu)}>☰</button></header><main id="top"><section className="hero"><div className="orb orb-a"/><div className="orb orb-b"/><div className="eyebrow"><span className="pulse"/> COMMUNITY-DRIVEN MINECRAFT</div><h1>Ton serveur.<br/><em>Notre univers.</em></h1><p className="hero-copy">NOVUS est un serveur Minecraft construit avec sa communauté. Propose une idée, construis-la, fais-la tester — et vois-la peut-être arriver en jeu à 21h30.</p><div className="hero-actions"><a className="button primary" href="/plugins">Explorer les plugins <span>→</span></a><a className="button ghost" href="#upload">Contribuer</a></div><div className="hero-status"><span>●</span> Pipeline communautaire <b>·</b> Prochaine release <strong>21:30</strong></div></section><section id="vision" className="section"><div className="section-head"><span>01 / PHILOSOPHIE</span><h2>Construire <em>ensemble.</em></h2></div><div className="feature-grid">{features.map(([i,t,p])=><article className="feature" key={t}><div className="feature-icon">{i}</div><h3>{t}</h3><p>{p}</p></article>)}</div></section><section id="security" className="security-section"><div className="section-head"><span>02 / TRUST LAYER</span><h2>Chaque contribution<br/><em>est inspectée.</em></h2></div><div className="security-layout"><div className="security-copy"><p>Un fichier communautaire n’est jamais exécuté directement sur la production. Il est isolé, analysé et testé avant toute décision.</p><div className="flow"><div>UPLOAD</div><b>→</b><div>QUARANTAINE</div><b>→</b><div>ANALYSE</div><b>→</b><div>STAGING</div></div><p className="muted">L’analyse automatisée assiste la décision. La validation humaine reste obligatoire.</p></div><div className="scan-card"><div className="scan-top"><span>🤖 NOVUS SCAN</span><span className="scan-id">COMMUNITY PIPELINE</span></div>{checks.map(([n,s,d])=><div className="check" key={n}><div><strong>{n}</strong><small>{d}</small></div><span>{s}</span></div>)}<div className="verdict"><span>GOUVERNANCE</span><strong>HUMAN REVIEW</strong></div></div></div></section><section id="upload" className="section upload-section"><div className="section-head"><span>03 / CONTRIBUER</span><h2>Une idée ?<br/><em>Envoie-la.</em></h2></div><div className="upload-card"><div className="dropzone" onClick={()=>document.getElementById('file').click()} onDragOver={e=>e.preventDefault()} onDrop={e=>{e.preventDefault();chooseFile(e.dataTransfer.files[0])}}><input id="file" type="file" accept=".jar,.zip" hidden onChange={e=>chooseFile(e.target.files[0])}/><div className="upload-icon">↑</div><h3>{file?file.name:'Dépose ton plugin ici'}</h3><p>{file?`${(file.size/1024/1024).toFixed(2)} MB · prêt pour la quarantaine`:'ou clique pour sélectionner un .jar ou .zip · 10 Mo max'}</p></div><div className="upload-bottom"><div><label className="author-label">Nom / pseudo (optionnel)</label><input className="author-input" value={author} onChange={e=>setAuthor(e.target.value)} placeholder="Noxo"/><strong>Avant 17h30</strong><span>pour la release du jour</span></div><button className="button primary" disabled={!file||status?.type==='loading'} onClick={submit}>{status?.type==='loading'?'Envoi…':'Envoyer en quarantaine'} <span>→</span></button></div>{status&&<div className={`submission-status ${status.type}`}><strong>{status.type==='success'?'✓ ':status.type==='error'?'⚠ ':''}{status.message}</strong>{status.result?.issueUrl&&<a href={status.result.issueUrl} target="_blank" rel="noreferrer">Voir la proposition ↗</a>}{status.result?.id&&<small>ID : {status.result.id}</small>}</div>}</div></section><section id="release" className="release-section"><div className="release-time">21<span>:30</span></div><div><span className="eyebrow">EVERY DAY / RELEASE WINDOW</span><h2>Le serveur évolue<br/><em>avec vous.</em></h2><p>17h30 ferme les contributions. Analyse, tests et revue suivent, puis la mise à jour publique arrive à 21h30 si elle est validée.</p></div></section></main><footer><div className="brand"><span className="brand-mark">N</span> NOVUS</div><span>Built with the community · 2026</span><a href="/plugins">Plugins</a><a href="/admin">Administration ↗</a></footer></div>}

createRoot(document.getElementById('root')).render(location.pathname.startsWith('/admin')?<Admin/>:location.pathname.startsWith('/plugins')||location.pathname.startsWith('/account')?<Community/>:<App/>);
